package com.transportista.sistemaguias.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AWSConfig {
    // AWS deshabilitado para pruebas locales
    // Cuando despliegues en EC2, descomenta las dependencias en pom.xml
    // y reemplaza esta clase con la versión completa de AWS
}